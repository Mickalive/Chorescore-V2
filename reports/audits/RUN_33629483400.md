# Audit RUN_33629483400 — V2-07 repair (third attempt)

- **Cycle**: 33629483400
- **Role**: builder
- **Decision**: **repair**
- **Active criterion**: V2-07 (APK Android final)

## Objective

REPAIR the V2-07 release blocker: the `android-emulator-runner` action executes each `script:` line as a separate `sh -c` process, so the `$package` shell variable does not persist across lines, causing `smoke-android-apk.sh` to receive an empty package argument and abort. The fix must either:

- **(A)** inline the package value per line — pass `${{ steps.contract.outputs.package }}` directly to `smoke-android-apk.sh` and set `CHORESCORE_E2E_PACKAGE="${{ steps.contract.outputs.package }}"` on each line that uses it; OR
- **(B)** make `smoke-android-apk.sh` fall back to the package from `app.json` when `$2` is empty (mirroring `scripts/e2e-android.js configuredAndroidPackage()`).

This is the **third** consecutive repair cycle. The previous two candidates produced zero delta on the target files.

## Trusted verification

Trusted verification exit code is **0**. The trusted report (`reports/audits/TRUSTED_33629483400.md`) shows `npm run check` green (typecheck clean, 608 tests across 16 suites passing), Expo export/prebuild and Gradle `assembleDebug` build successful.

**Important scope note**: the trusted report only runs `npm run check`, `expo export`, `expo prebuild`, and `gradlew assembleDebug`. It **never invokes the `android-emulator-runner` script block** that contains the release blocker. Therefore the exit code 0 validates the build baseline but does **not** validate the release blocker fix.

## Candidate inspection

The candidate checkout was compared against the pristine accepted tree (`/home/runner/work/_temp/accepted-pristine`):

- `diff .../.github/workflows/chorescore-v2-factory.yml ...` → **IDENTICAL**
- `diff .../.github/scripts/smoke-android-apk.sh ...` → **IDENTICAL**

The candidate made **no changes** to the release scripts or the workflow. The only untracked file is the trusted verification report itself. This is the third consecutive cycle with zero delta on the target files.

## Release blocker still present

The buggy pattern remains in `.github/workflows/chorescore-v2-factory.yml` (lines 204–210):

```yaml
script: |
  package="${{ steps.contract.outputs.package }}"
  echo "Using validated Android package: $package"
  bash "$GITHUB_WORKSPACE/.github/scripts/smoke-android-apk.sh" "${{ steps.build.outputs.apk }}" "$package" "$RUNNER_TEMP/v2-smoke"
  mkdir -p "$RUNNER_TEMP/v2-e2e"
  CHORESCORE_APK_PATH="${{ steps.build.outputs.apk }}" CHORESCORE_E2E_PACKAGE="$package" CHORESCORE_E2E_OUTPUT="$RUNNER_TEMP/v2-e2e" npm run e2e:android
  jq -e '.status=="pass"' "$RUNNER_TEMP/v2-e2e/result.json" >/dev/null
```

Because `android-emulator-runner` runs each line as a separate `sh -c`, the `package=...` assignment on line 205 does not persist to lines 207/209. `smoke-android-apk.sh` line 3 still requires a non-empty `$2` (`package="${2:?package}"`) and has **no** `app.json` fallback, so it aborts with an empty package argument.

Neither of the two permitted fixes was implemented:
1. Inline the package value per line — **not done** (still uses `$package`).
2. Add an `app.json` fallback in `smoke-android-apk.sh` — **not done**.

## Findings

| # | Path | Problem | mustFix |
|---|------|---------|---------|
| 1 | `.github/workflows/chorescore-v2-factory.yml` | Release blocker unfixed (third attempt): `$package` still assigned on one line and referenced on later lines across separate `sh -c` processes; `smoke-android-apk.sh` receives an empty package and aborts. No inline-per-line value and no `app.json` fallback added. | **yes** |

## Conclusion

Trusted verification passed (exit code 0), but it does not exercise the `android-emulator-runner` script block, and the candidate did not implement the required fix. The V2-07 release blocker remains, so a mandatory mustFix finding is open. The decision is **repair**.
