# Audit RUN 33338762850 — V2-02 REPAIR (local persistence)

- **Cycle** : 33338762850
- **Critère actif** : V2-02 REPAIR — Fix local persistence
- **Rôle** : Independent Auditor
- **Décision** : **accept**
- **Arbre de référence** : `/home/runner/work/_temp/accepted-pristine`
- **Candidat** : checkout courant (`HEAD e991e0b` + candidat)

## 1. Contexte et périmètre

La tâche demandait, sans nouveau scope fonctionnel :

1. créer des repositories persistants AsyncStorage pour `entries`, `persistentTasks`, `chronoTimer` ;
2. brancher `AppContext.tsx` sur ces repositories ;
3. remplacer le test chrono tautologique par un test non tautologique prouvant la survie des données à la recréation d'instance ;
4. conserver les 183 tests existants verts et `npm run check` vert.

## 2. Diff candidat réel

| Fichier | Changement |
|---|---|
| `src/infrastructure/repositories/PersistentRepositories.ts` | **nouveau** : `PersistentEntryRepository`, `PersistentPersistentTaskRepository`, `PersistentChronoTimerRepository` (AsyncStorage, clés `@chorescore/…`, `safeParse` défensif, tri décroissant `occurredAt` conservé) |
| `src/features/app/AppContext.tsx` | branche les 3 repositories persistants ; les autres restent InMemory (hors périmètre) |
| `__tests__/infrastructure/persistence.test.ts` | **nouveau** : 12 tests non tautologiques (écriture instance 1 → recréation instance 2 → lecture) |
| `__tests__/application/V2-02-acceptance.test.ts` | remplace les 2 tests chrono tautologiques par des tests de recréation d'instance |
| `jest.setup.js` | **nouveau** : mock officiel AsyncStorage pour Jest |
| `package.json` / `package-lock.json` | ajout `@react-native-async-storage/async-storage@2.2.0` (version bundle Expo SDK 57 exacte) |

Aucun fichier de constitution, gouvernance, roadmap, tâches ou état de release n'est modifié. Aucun nouveau scope fonctionnel.

## 3. Vérifications exécutées

### 3.1 Vérification trusted

`reports/audits/TRUSTED_33338762850.md` : **exit code 0** — `BUILD SUCCESSFUL`, tâches `react-native-async-storage_async-storage` compilées (Java + bundle), APK debug assemblé. Le module natif AsyncStorage est réellement intégré au build Android.

### 3.2 Checks indépendants

| Check | Résultat |
|---|---|
| `npm run typecheck` | vert (`tsc --noEmit`) |
| `npm test` | 9 suites, **195 tests passés** (183 préexistants + 12 nouveaux) |
| `npm run check` | vert |
| Comptage arbre pristine | 183 tests confirmés |
| Version AsyncStorage vs `expo/bundledNativeModules.json` | 2.2.0 = 2.2.0 |
| Conformité interfaces | `EntryRepository`, `PersistentTaskRepository`, `ChronoTimerRepository` satisfaits exactement |
| Hydratation UI | `AddTaskScreen` recharge `getVisibleEntries` / `getPersistentTasks` / `getChronoState` au montage → les données persistées sont relues depuis AsyncStorage |
| Test indépendant niveau app (auditeur) | écriture via `ChoreScoreApp` instance 1 (repos persistants) → recréation complète instance 2 → lecture entrées/tâches/chrono OK ; update + delete survivent à la recréation (2/2 passés) |
| Lint | les 12 erreurs sont toutes préexistantes (fichiers/lignes non touchés par le candidat) ; `lint` ne fait pas partie de `npm run check` |

### 3.3 Non-tautologie des tests

L'ancien test lisait deux fois la même `Map` en mémoire de la même instance : il passait même avec zéro persistance. Les nouveaux tests écrivent via une instance puis lisent via une **instance fraîche** partageant le store du mock AsyncStorage (équivalent de deux lancements partageant le même disque). Si les données vivaient seulement dans la mémoire d'instance, les lectures échoueraient. La persistance disque réelle est fournie par le module natif AsyncStorage, compilé avec succès dans le build Android vérifié par le trusted job.

## 4. Critères d'acceptation de la tâche

1. ✅ Entries, persistentTasks et chronoTimer persistent sur disque (AsyncStorage).
2. ✅ Les données survivent au redémarrage (test non tautologique : write → recreate instance → read back).
3. ✅ L'état du chrono persiste et reprend après redémarrage (`getChronoState` au montage de l'écran).
4. ✅ Les 183 tests existants passent sans régression (195 au total).
5. ✅ `npm run check` vert.
6. ✅ Invariants design et V2-00/V2-01 préservés (aucune modification de ces zones ; tests V2-01 verts).

## 5. Findings

Aucun finding `mustFix`.

Points examinés et écartés (non bloquants) :
- **Mock AsyncStorage en mémoire** : approche standard ; le test prouve le passage par la couche de stockage à travers la recréation d'instance ; la persistance disque réelle est assurée par le module natif compilé.
- **Classes InMemory conservées** : utilisées uniquement comme fixtures de test ; le câblage production utilise les repositories persistants.
- **Tests d'acceptation chrono** : les flux app `startChrono`/`stopChrono`/`clearState` restent couverts ; la persistance est prouvée au niveau repo et au niveau app (vérification indépendante).
- **Atomicité read-modify-write AsyncStorage** : acceptable pour un usage local mono-utilisateur dans ce périmètre de réparation.

## 6. Décision

**accept** — la vérification trusted est passée (exit code 0) et aucun finding `mustFix` ne reste pour le critère actif. La réparation V2-02 est complète, non tautologique, sans nouveau scope et sans régression des invariants acquis.