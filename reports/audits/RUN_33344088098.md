# Audit RUN 33344088098 — V2-03 REPAIR

## Decision: ACCEPT

## Objective
V2-03 REPAIR — Fix the mustFix finding: in Free mode the year/all-time period buttons were disabled, making the contextual Premium upsell unreachable. Make them tappable so tapping triggers the upsell while keeping current civil-month data visible.

## Trusted verification
- Exit code: **0** (`reports/audits/TRUSTED_33344088098.md` — `npm run check` green, Android debug build `BUILD SUCCESSFUL`).
- Local `npm run check`: typecheck clean, **235 tests pass** (227 existing + 8 new).

## What was verified

### The mustFix is fixed
1. **Buttons tappable in Free** — `ScoreScreen.tsx` `onPress={() => handlePeriodChange(p)}` no longer gated by `!isDisabled`. The `isDisabled` variable is now used only for visual styling (dimmed + 🔒), not to block taps.
2. **Tap triggers upsell** — `handlePeriodChange` sets `needsPremium=true` when `!isPremium && (year || all-time)`, rendering the contextual Premium upsell card.
3. **Current civil-month data stays visible** — `loadData` now always loads score/history (removed the `if (!needsPremiumPeriod)` guard), and the balances/history sections no longer hide behind `!needsPremium`. The app layer (`filterEntriesByPeriod`) limits Free to the current civil month for all periods, so year/all-time show current-month data below the upsell.
4. **No Premium regression** — in Premium mode `needsPremium` stays false and the full archive is shown (verified by test and code path).
5. **Weighted Premium-only section** still correctly gated by `!needsPremium && isPremium`.

### Scope
Diff is minimal and in scope: only `src/features/household/ScoreScreen.tsx` and `__tests__/application/V2-03-acceptance.test.ts`. No governance/constitution/roadmap/task files touched. No V1 carry-over, no fake integrations, no domain/provider coupling.

### Tests
- 8 new tests in SECTION 10: current-month data visible for year/all-time in Free, Premium full-archive no-regression, balance correctness when switching back to month, and upsell-reachability logic.
- All 235 tests pass; typecheck green.

## Findings

### Non-blocking (mustFix: false)
- **Tautological upsell-reachability tests** (`__tests__/application/V2-03-acceptance.test.ts`): the "simulation" tests re-implement the `handlePeriodChange` boolean logic inline rather than rendering the actual `ScoreScreen` component. They do not strictly "prove upsell reachability from the Free UI" as literally stated. Non-blocking because the codebase has no component-rendering test infrastructure, the actual fix is verifiably correct by inspection, and the data-visibility tests exercise the real `ChoreScoreApp` methods. Recommended to replace with a real render test if component-rendering infrastructure is added later.

## Conclusion
The previous mustFix finding is resolved: year/all-time buttons are tappable in Free, tapping triggers the contextual Premium upsell, and current civil-month data remains visible. Trusted verification passed (exit 0) and there is no remaining mustFix finding. **Decision: accept.**
