# Audit Indépendant — Cycle 33323640740

**Critère actif :** V2-00 — Réparation des deux findings `mustFix` de l'audit 33292303527
**Décision :** `accept`
**Rôle audité :** builder

---

## Périmètre

Audit contradictoire du candidat de réparation V2-00 contre la constitution (`MAIN_PROMPT.md`), `governance/RELEASE_DEFINITION.json`, `docs/ROADMAP.md`, `docs/PRODUCT_BLUEPRINT.md`, `docs/DESIGN_CONTRACT.md`, `docs/DESIGN_BRIEF.md`, `docs/MONETIZATION.md`, `docs/SUBSCRIPTION_REFERENCE_V1.md`, `docs/DATA_PRODUCT_PRIVACY.md`, `docs/QUALITY_GATES.md`, `docs/REFERENCE_SCENARIOS.json`, la tâche active (REPAIR V2-00 : corriger les deux findings mustFix, aucune nouvelle feature) et le diff candidat réel. L'arbre accepté pristine (`/home/runner/work/_temp/accepted-pristine`) ne contient que le control-plane humain ; tout le code applicatif est le candidat greenfield réparé.

**Vérification de confiance :** exit code `0` (rapport `reports/audits/TRUSTED_33323640740.md`) — `npm run check` vert, `npx expo install --check` « Dependencies are up to date », export Android OK, `./gradlew :app:assembleDebug` **BUILD SUCCESSFUL** en 18m40s.

## Checks indépendants exécutés

| Check | Résultat |
|-------|----------|
| `npm run check` (typecheck + 67 tests jest) | ✅ vert (5 suites, 67 tests, 0 échec) |
| `npx expo install --check` | ✅ « Dependencies are up to date » (seuls react-native-worklets et typescript exclus via `expo.install.exclude`) |
| `package.json` vs `expo/bundledNativeModules.json` | ✅ les 7 paquets précédemment divergents sont alignés SDK 57 |
| `package-lock.json` régénéré et cohérent | ✅ lockfileVersion 3, versions résolues conformes à package.json |
| Versions installées | ✅ expo 57.0.18, expo-router 57.0.17, expo-constants 57.0.16, expo-font 57.0.2, expo-splash-screen 57.0.8, expo-linking 57.0.8, expo-status-bar 57.0.1, react-native-safe-area-context 5.7.0, jest-expo 57.0.5 |
| `./gradlew :app:assembleDebug` | ✅ BUILD SUCCESSFUL (vérification de confiance, exit code 0) ; APK produit (`android/app/build/outputs/apk/debug/app-debug.apk`) |
| Inspection du fait analytics émis par `createEntry` | ✅ uniquement `durationMinutes`, `beneficiaryCount`, `hasPersistentTask` — aucun ID opérationnel |
| Test ajouté exerçant `createEntry` avec analytics activé | ✅ vérifie l'absence de tout ID opérationnel et la présence des données minimisées |
| Test indépendant de l'auditeur (scan profond clés + valeurs) | ✅ aucune fuite d'identifiant ni de texte libre dans le fait sérialisé |
| Grep `emitFact` | ✅ un seul point d'émission dans le produit (`createEntry`) |
| Régression des invariants acceptés | ✅ architecture, ports, adapters honnêtes, analytics secondaire désactivable, shell de navigation, charte visuelle, placeholders V2-02/V2-03/V2-04 intacts |

## Résolution du finding mustFix n°1 — Versions de paquets Expo devinées

**Avant (audit 33292303527) :** expo-constants ~17.0.3, expo-font ~13.0.4, expo-splash-screen ~0.30.3, expo-linking ~7.0.3, expo-status-bar ~2.2.3, expo-router ~4.0.4, react-native-safe-area-context ~5.4.0 → `./gradlew :app:assembleDebug` échouait sur `:expo-constants:compileDebugKotlin`.

**Après (candidat actuel) :**

| Paquet | Attendu SDK 57 (`bundledNativeModules.json`) | Candidat | Installé |
|--------|----------------------------------------------|----------|----------|
| expo-constants | ~57.0.16 | ~57.0.16 | 57.0.16 |
| expo-font | ~57.0.2 | ~57.0.2 | 57.0.2 |
| expo-splash-screen | ~57.0.8 | ~57.0.8 | 57.0.8 |
| expo-linking | ~57.0.8 | ~57.0.8 | 57.0.8 |
| expo-status-bar | ~57.0.1 | ~57.0.1 | 57.0.1 |
| expo-router | ~57.0.17 | ~57.0.17 | 57.0.17 |
| react-native-safe-area-context | ~5.7.0 | ~5.7.0 | 5.7.0 |

- Lockfile régénéré (lockfileVersion 3) et cohérent avec `package.json` (vérifié paquet par paquet).
- `npx expo install --check` : « Dependencies are up to date » — plus aucune divergence signalée.
- `./gradlew :app:assembleDebug` : **BUILD SUCCESSFUL** (vérification de confiance, exit code 0, 353 tâches exécutées), APK debug présent.

**Statut : corrigé et vérifié.**

## Résolution du finding mustFix n°2 — `householdId` dans le fait analytics

**Avant :** `createEntry` émettait `data.householdId` dans le fait analytics, violant le contrat du port `ResearchAnalyticsGateway` (« Never include: userId, householdId, memberId, email, free text, IP, device ID ») et `docs/DATA_PRODUCT_PRIVACY.md`.

**Après (candidat actuel) :** `src/application/use-cases/ChoreScoreApp.ts` (lignes 135-148) n'émet que :

```ts
data: {
  durationMinutes: data.durationMinutes,
  beneficiaryCount: data.beneficiaryMemberIds.length,
  hasPersistentTask: data.persistentTaskId != null,
}
```

- Aucun `householdId`, `memberId`, `performedByMemberId`, `beneficiaryMemberIds`, `label`, `email`, `createdBy`, `userId`, `notes`, `name`, `ownerId`, `persistentTaskId`, `id`, `occurredAt` dans le fait.
- Nouveau test ajouté (`__tests__/application/ChoreScoreApp.test.ts`, « should emit no operational IDs in analytics fact when creating entry ») : exerce `createEntry` avec analytics activé, vérifie l'absence de tout ID opérationnel et la présence des données minimisées (`durationMinutes: 30`, `beneficiaryCount: 2`, `hasPersistentTask: false`).
- Test indépendant de l'auditeur (fichier temporaire, retiré après exécution) : scan profond des clés **et** des valeurs du fait sérialisé avec des identifiants volontairement reconnaissables (`h-secret-123`, `m-alex-42`, `m-sam-7`, `pt-9`, « Vaisselle du soir ») — aucune fuite. Le fait est exactement `{durationMinutes: 45, beneficiaryCount: 2, hasPersistentTask: true}`.
- Grep `emitFact` : un seul point d'émission dans le produit (`createEntry`) ; `completeTodo` et les autres use-cases n'émettent aucun fait.

**Statut : corrigé et vérifié.**

## Points conformes (régression)

- Architecture `domain/application/infrastructure/UI` intacte ; le domaine ne dépend d'aucun fournisseur externe.
- Ports Auth/Entitlement/Billing/SystemShare/Notification/Calendar/SecureStorage/Sync/ResearchAnalytics intacts, contrat analytics documenté.
- Adapters locaux honnêtes : aucun faux OAuth/paiement/push/sync/analytics ; analytics secondaire désactivable, ne conditionne aucune fonction produit.
- Shell de navigation foyers → `Ajouter une tâche | Score | To-do`, charte visuelle chaleureuse, placeholders explicites V2-02/V2-03/V2-04 — inchangés.
- `npm run check` vert : 67 tests déterministes (66 précédents + 1 nouveau test de non-fuite analytics).

## Findings

Aucun finding `mustFix` restant.

### Non bloquant (reporté, hors périmètre)

- **Path :** `src/application/use-cases/ChoreScoreApp.ts`
- **Problème :** `createHousehold` appelle `canUseFeature('new-household', 'create-household')` avec la chaîne littérale `'new-household'` comme `householdId` (entitlement résolu contre un ID de foyer inexistant). Déjà signalé à l'audit 33292303527, explicitement différé en V2-01 ; le cycle actuel est une réparation ciblée (« aucune nouvelle feature »).
- **mustFix :** false
- **Correction exigée :** en V2-01, résoudre l'entitlement de création/possession au niveau du compte plutôt que contre un ID de foyer factice.

## Conclusion

Les deux findings `mustFix` de l'audit 33292303527 sont corrigés et vérifiés indépendamment :

1. **Dépendances Expo alignées SDK 57** : versions conformes à `bundledNativeModules.json`, lockfile régénéré, `npx expo install --check` sans divergence, `./gradlew :app:assembleDebug` **BUILD SUCCESSFUL** (exit code 0 de la vérification de confiance), APK produit.
2. **Fait analytics minimisé** : `createEntry` n'émet que `durationMinutes` / `beneficiaryCount` / `hasPersistentTask` ; test dédié exerçant le chemin réel `createEntry` avec analytics activé ; vérification indépendante par scan profond sans fuite d'identifiant.

`npm run check` reste vert (67 tests). Aucune nouvelle feature introduite. La vérification de confiance a un exit code `0`.

**Décision : `accept`**