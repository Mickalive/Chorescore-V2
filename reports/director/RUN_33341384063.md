# Director RUN 33341384063

- **Cycle** : 33341384063
- **Critère actif** : V2-03 — Score, équilibres et fenêtre gratuite
- **Décision audit** : repair
- **Décision Director** : continue

## Contexte

L'audit indépendant du cycle 33341384063 a rendu un verdict **repair** sur V2-03. Le candidat ajoute les sélecteurs de période/filtre, l'algorithme de soldes +D/-D/N à somme nulle, les compensations pair-à-pair, les graphes nommés, la section pondérée Premium secondaire et l'historique contextuel filtré. La vérification trusted est passée (exit code 0), 227 tests sont verts, le typecheck est propre, le build Android est OK.

## Finding mustFix

**Upsell contextuel Année/Depuis le début inaccessible en Free**

Dans `ScoreScreen.tsx`, les boutons de période Année/Depuis le début sont désactivés en mode Free (`Pressable` avec `onPress={() => !isDisabled && handlePeriodChange(p)}` et icône cadenas). Taper ne fait rien, donc l'upsell contextuel Premium pour ces périodes est inatteignable via l'UI normale.

**Correction exigée** :
1. Rendre les boutons Année/Depuis le début tappables en mode Free
2. Au tap, déclencher `setNeedsPremium(true)` pour afficher la carte d'upsell contextuel
3. Conserver les données du mois civil courant visibles pendant l'affichage de l'upsell
4. Ajouter/ajuster un test d'acceptation prouvant que l'upsell est atteignable depuis l'UI Free

## Findings non bloquants notés

- `ArchiveMessage` onUpgrade est un no-op (CTA inerte) — sera résolu en V2-05
- « Somme des soldes » affiche toujours « 0 min » par construction — curiosité UX mineure

## État conservé

V2-03 reste `in_progress`. Le baseline du candidat déjà committée par le trusted shell est préservée. Le Builder doit appliquer uniquement la réparation demandée sans élargir le scope.

## Prochaine action

Le Builder doit corriger le finding mustFix dans ScoreScreen.tsx, ajouter le test d'acceptation, et soumettre un nouveau candidat pour audit.
