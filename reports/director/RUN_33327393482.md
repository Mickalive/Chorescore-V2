# Director Run — Cycle 33327393482

**Decision:** `continue`
**Criterion:** V2-01 — Identité, foyers, abonnement et Options (repair)
**Next step:** Builder assigned the 3 mustFix fixes for V2-01

---

## Summary

Audit for cycle 33327393482 returned `repair` for V2-01. The WIP repair baseline (commit `9415dc6`) is preserved with a green `npm run check` (6 suites, 120 tests). The codebase is solid on the structural elements but has 3 mustFix findings that must be resolved before V2-01 can be accepted.

## V2-00 Evidence Preserved

All V2-00 evidence remains intact:
- Architecture greenfield: domain/application/infrastructure/UI separation
- All ports defined with honest local adapters (Auth, Entitlement/Billing, SystemShare, Notification, Calendar, SecureStorage, Sync, ResearchAnalytics)
- ResearchAnalyticsGateway secondary, disableable
- Shell navigation: Foyers → Ajouter une tâche | Score | To-do
- Visual design: warm cream, terracotta, ScreenContainer/Card/Button/Text
- 120 deterministic tests passing, typecheck clean

## V2-01 WIP Baseline Preserved

The following V2-01 work is confirmed present and tested:
- **Entities:** User (id, email, displayName), Household, Member, Membership (MEMBER/OWNER), Account (ownedFreeHouseholdId)
- **Identity:** session locale testable, userId stable par email, `Fait par` is entry data not identity switch
- **AuthGateway:** email/Google/Facebook ports; adapters honest (Google/Facebook return null)
- **Entitlement creation at account level:** createHousehold consults AccountRepository (no 'new-household' fake ID)
- **Multiple invitations:** joinHousehold creates MEMBER membership without account-level limit
- **PRICING configurable:** TRIAL_DAYS=30, STANDARD_MONTHLY_EUR=2.99, STANDARD_MEMBER_LIMIT=7, PRO_MONTHLY_EUR=5.99, PRO_MEMBER_THRESHOLD=8
- **resolveEffectivePlan:** returns 'pro' at 8+ members

## Findings Requiring Repair (3 mustFix)

### 1. Trial Expiry Not Wired to Entitlement Resolution

- **Path:** `src/infrastructure/local/LocalEntitlementAdapter.ts`
- **Problem:** `getEntitlement` returns stored entitlement (`plan:'trial'`) without checking `trialEndsAt`. A household whose 30-day trial expired keeps Premium capabilities indefinitely. Violates criterion 6: "trial ends and returns to Free without data destruction."
- **Required fix:** Wire `trialEndsAt` check into `getEntitlement`. When expired, return Free entitlement. Add deterministic test simulating trial expiry.

### 2. Options Screens Not Implemented

- **Path:** `app/index.tsx`
- **Problem:** Options personal (profile, notifications, privacy, legal, preferences) and Options household (subscription, administration, members, permissions) are placeholders with `onPress={() => {}}`. No routes or screens exist. Violates criteria 4 and 5.
- **Required fix:** Implement both Options screens with real navigation. Options household visible only to owner/payer.

### 3. getAccountEntitlement Demo-Free Mode Incorrect

- **Path:** `src/infrastructure/local/LocalEntitlementAdapter.ts`
- **Problem:** `getAccountEntitlement` scans global entitlements map for any 'free' plan instead of consulting `AccountRepository`. Inconsistent with `createHousehold` and can produce wrong "Create household" button state. Violates criterion 10.
- **Required fix:** Resolve via `AccountRepository.ownedFreeHouseholdId`, consistent with `createHousehold`.

## Next Steps

- **Builder:** Fix the 3 mustFix findings (trial expiry wiring, Options screens, getAccountEntitlement)
- **V2-01** remains active
- **V2-00** evidence locked
- No criteria skipped, no outcomes weakened
