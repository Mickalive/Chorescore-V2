{
  "schemaVersion": 1,
  "milestone": "v2-rc",
  "builder": {
    "enabled": true,
    "criterionId": "V2-00",
    "objective": "REPAIR V2-00: corriger les deux findings mustFix de l'audit cycle 33292303527. Aucune nouvelle feature. (1) Remplacer les versions de paquets Expo devinées avec l'ancien schéma par les versions alignées SDK 57 (utiliser npx expo install pour aligner automatiquement expo-constants, expo-font, expo-splash-screen, expo-linking, expo-status-bar, expo-router, react-native-safe-area-context). Régénérer le lockfile. Vérifier que ./gradlew :app:assembleDebug réussit et que npx expo install --check ne signale plus de divergence. (2) Retirer householdId (et tout autre identifiant opérationnel) du fait analytics émis par ChoreScoreApp.createEntry — ne conserver que des données minimisées (durée, nombre de bénéficiaires, présence de PersistentTask). Ajouter un test qui exerce createEntry avec analytics activé et vérifie l'absence de tout ID opérationnel (householdId, memberId, label, email, etc.). Vérifier que npm run check reste vert.",
    "acceptance": [
      "./gradlew :app:assembleDebug (ou assembleRelease) réussit — la compilation native Android n'échoue plus",
      "npx expo install --check ne signale plus de paquet hors alignement SDK 57",
      "npm run check (typecheck + tests) reste vert",
      "createEntry n'émet aucun identifiant opérationnel (householdId, memberId, label, email, IP, device ID) dans le fait analytics",
      "Un test exerce createEntry avec analytics activé et vérifie l'absence de tout ID opérationnel dans data[]",
      "Aucune régression sur les 66 tests existants"
    ]
  }
}
